# RAILWISE AI / Survey 迁移矩阵

基线：用户提供的《RAILWISE AI ｜ Survey 产品收敛与命名迁移总计划》。
本轮改显示名称、品牌图标、增量任务合同与专业导航；不授权任何公开版本发布。

| 旧项目 | 新状态 | 原因 | 数据保留 | 用户操作 |
| --- | --- | --- | --- | --- |
| WorkWise 平台显示名 | RAILWISE AI；菜单、启动页、设置页、关于页共用品牌配置 | 平台命名统一 | 不调用 app.setName，不改正式安装目录和用户数据路径 | 无 |
| WorkWise / W 图标 | RAILWISE 的 R 字形、平行轨线和基准点；统一 SVG、PNG、Dock、托盘、ICO、ICNS | 显示品牌迁移 | 保留 workwise.* 路径及所有包/数据标识；Git 保留旧资源 | 候选包中确认显示效果 |
| Survey 工作台 | RAILWISE Survey / 工程测量内业 | 明确当前专业边界 | engineering 路由、线程 domain 和 projectId 不变 | 无 |
| monitoringType=deformation | 读取为变形监测 taskType | 兼容旧监测项目 | 不重写数据库；旧字段、单位、阈值原样保留 | 可显式修改任务类型 |
| 旧控制网/水准/导线/设站/GNSS 类型 | 映射到对应 taskType | 避免全部错误归为变形监测 | 只读推导；未知自定义类型原样保留 | 未识别类型由用户选择 |
| 新建项目默认监测类型 | 新建控制网任务；UI 明示类型选择 | 通用内业入口 | 不改变已有项目默认值 | 确认类型、基准与规范 |
| 十个平级视图与独立 AI 阶段 | 四个生产阶段，阶段内工具，持续对话 | 跟随生产链 | 保留旧 Tab ID 与 AI 线程恢复路径 | 无 |
| 有输出即显示已审查 | 仅有效且 approved 的 manifest 显示已审查 | 防止候选成果误交付 | 历史文件、manifest 与哈希不修改 | 仍须专业复核 |
| 候选包显示名 | RAILWISE AI Candidate + 源码摘要 | 使测试包体现新品牌 | 旧候选名称、独立数据及正式更新源保持隔离；已有真实私有 HTTPS 更新流程，仍须逐精确候选留证 | 仅安装隔离候选 |
| npm/package、bundle ID、仓库、下载源、更新通道 | 保持现有技术标识 | 升级与数据兼容 | 原值不变 | 无 |
| 插件、Skill、MCP、凭据、用户目录 | 保留全部 | 无目录/许可迁移 | 本轮不增删、不搬迁、不读取凭据 | 无 |

验证必须分别执行根项目与 kun 的测试；根 Vitest 配置不包含 kun 测试。
打包 GUI、签名/公证和真实 updater 往返须绑定同一候选源码；历史包证据不替代本轮验收。

私有更新环境已按[隔离原生 updater 流程](qa/PRIVATE_SURVEY_UPDATER_ACCEPTANCE.md)多次完成真实下载、安装和重启；它不等于真实旧用户数据全量迁移或当前候选验收通过。剩余范围见[总计划未完成清单](qa/RAILWISE_SURVEY_REMAINING_WORK.md)。

## 实机复测发现并修复（2026-09-19）

- 控制网的默认导入/分析曾错误进入监测时序页；新增专业来源与精度子页，四阶段跳转保留所选测量网络。
- 未校核的可解析来源曾能直接点击平差；界面现在要求先完成质量校核。
- 一次性平差曾因没有观测期次被标成仅审计，无法进入成果链；观测期次要求现仅用于多期变形比较，仍保留当前来源准入和有效性门禁。
- 摘要点数改用网络中去重后的已知/未知点；质量 revision 更新会刷新摘要。
- GSI 的显式控制点映射原先只有 Runtime 入口；候选界面增加米制控制点输入，映射改变会生成不同的导入幂等键。
- 设置帮助与运行时显示文案改用统一品牌变量；仓库链接、旧来源格式和系统凭据提示保留技术旧名。

上述修复需要新候选包复验，不追溯修改旧候选包验收结论。

图标由固定版本 resvg 从无字体依赖的 SVG 直接生成；生成器不启动应用，不依赖隐藏窗口截图。新增构建依赖采用 MPL-2.0，属于开发工具，不进入 Runtime。

## 审批信息增量兼容（2026-09-19）

| 旧项目 | 新状态 | 原因 | 数据保留 | 用户操作 |
| --- | --- | --- | --- | --- |
| 没有参数、结果绑定、产物或可逆性说明的历史 AI 计划 | 可读、不可直接审批执行 | 无法证明实际调用与原审批范围一致 | 历史 JSON 不重写；不执行旧计划 | 在原操作范围内重建，补齐输入并重新审查 |
| 调用时由模型补选网络或成果 | Runtime 固定已审参数及前序运行 ID | 防止批准后改变输入、跳步或误读另一轮结果 | 新增步骤证据表，仅存参数及结果 ID/revision | 在卡片中审查参数 |
| AI 项目修改建议 | 单独的前后对照确认卡 | 参数建议不能直接修改项目 | 新增建议记录，保留拒绝/过期状态；确认使用原项目修订接口 | 确认或不采用；上下文变化后重新提议 |

对应回归包括真实 Survey 与监测工具链导出、跨项目拒绝、参数篡改拒绝、重复操作幂等、历史草稿保留，以及确认卡的过期和拒绝行为。整包 UI 与真实模型验收单独记录。
