# f404608 候选验收失败及修正

2026-09-19，从 DMG 隔离安装 `f404608d8e0d38a21f72a85618f0fd77e6755a74`；版本 0.5.0，Developer ID 严格/深层签名通过。没有公证票据，stapler 65；已有系统 Gatekeeper override 不构成公证通过。两条 GitHub workflow（35426889743、35426888038）通过，但真实 IN2 包内 Runtime 检查失败，因此候选验收失败。

GSI 包内 Runtime 成果检查通过。IN2 导入和校核通过，生成 XY 椭圆时因 `XY cofactor is not symmetric positive semidefinite` 阻断，没有将失败结果作为可交付成果。原签名包未修改。

在独立临时副本中，仅记录维数与相对误差的诊断表明：73 参数网络的逆矩阵 XY 块最大相对不对称约 1.069e-13，超过新增椭圆的 64 × machine epsilon（约 1.421e-14），但小于既有 Cholesky 协方差校核采用的 1e-12。

修正统一使用既有协方差对称容差，规范化比较以保留极小量级；只有通过对称校核才平均两个非对角元。不改原求解矩阵、不改坐标、不放宽持久结果哈希。负方差、明显不对称和非正定矩阵仍被拒绝。新增 6×6 Hilbert 矩阵解析逆的回归，在旧源码上失败、修复后通过；验证轴长/轴向及原矩阵不变。

修正后的编译 Runtime 在相同 Electron 下运行该真实 IN2：三格式成果、draft manifest、哈希检查及 33 点 OU2 独立对照通过，最大平面坐标差 0.063415839 mm。该临时编译代码验证不是新的精确包验收；修正版仍需重新打包、安装和 GUI 验证。真实数据与工程坐标未加入仓库。

英文深色 GUI 通过原生文件选择器导入明确合成的 12-byte 畸形 GSI：来源保持 archive-only，质量/平差按钮禁用；未知格式诊断、来源处置和嵌套准入消息仍有中文，见 [实机失败截图](./parser-english-failure.jpg)。修正补齐未知格式原文映射和限定的 disposition/code 外壳显示适配，未修改原始消息、源文件或点号。准备的 COSA .NET 48 条译文未写入应用，因为进一步检查确认该独立原型尚未接入界面；不计作用户可见 locale 修复。
