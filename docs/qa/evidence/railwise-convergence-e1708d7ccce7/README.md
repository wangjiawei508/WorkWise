# e1708d7 精确候选安装包与真实升级验收

日期：2026-09-20。源码 `e1708d7ccce7e134c502c3b82bc16e3efcd3290b`，隔离候选版本 `0.5.0`，macOS arm64。本记录覆盖具体已执行项目，不表示总计划、专业验收或公开发布已完成。

## 包与升级身份

来自 [Actions 35459471768](https://github.com/wangjiawei508/WorkWise/actions/runs/35459471768) 的目标附件 `10589583533`，附件 SHA-256 `ddb45749bab1e219eacc4829f3314911076bc5a7f15a82a6d163b011cc4155f8`。本机安装路径为 `/private/tmp/railwise-survey-e1708d7/Applications/RAILWISE AI Candidate e1708d7ccce7.app`。

- DMG SHA-256：`3aeb2d88286a3f91f517832c907065f820318ee757c87061f90b2319714229c1`。
- 本机 ASAR：`fcaa65bb42d526bffe33a860886e83f3863e8b7c44cfc18fca698d41c5bb6a18`，与真实 updater 安装后的 ASAR 完全相同。
- 本机 Developer ID 签名、stapler 公证票据校验均退出 0。主机 spctl 仍显示 `security disabled`；启用 Gatekeeper 的验证来自 hosted runner，不能混称为本机已开启。
- [真实 updater 六阶段报告](./updater-review.md)通过下载、安装、重启和哨兵保留；同源码 0.0.0 基线是隔离探针，不是历史用户数据迁移。

## 已执行 GUI 项目

| 项目 | 实际结果 |
| --- | --- |
| 真实 GSI | 显式输入 6 个控制高程，27 点、28 观测；校核、平差、DOCX/PDF/XLSX、草稿 manifest 和五项 fresh verification 通过 |
| 错误网型 | 首次误按默认平面网导入，高差观测缺平面坐标被 59 个阻断项拒绝；随后显式选择水准网重新导入通过，旧网络保留 |
| 新统计诊断 | 读取 28 条外部学生化 t；全模型自由度 7、删除后 6；假设未验证、判定未执行；最后一条记录 `record-156` 成功定位到 Runtime 唯一字节范围 |
| JSON 保存 | 原生另存为取消后无成功提示；再次保存成功，15,491 字节，SHA-256 `877ae5887303627c02c8fba626514a35f0c13667c26e9f905e0f9b253c04caef`；项目/网络/运行/结果/输入/算法绑定与持久记录一致，计算摘要另用规范序列化独立重算相同 |
| 真实 IN2 | 33 点、11 测站、160 观测；平差、三格式、独立草稿及五项 fresh verification 通过；未向非水准网提供水准诊断入口 |
| 双语与尺寸 | 中文浅色、英文深色均检查；常规 1171×768、最大化 1490×768；本轮缩窄自动操作未成功，不能记作窄窗口通过 |
| 重启恢复 | 原生退出，确认进程结束后按同一隔离环境重启；再次进入内业恢复所选 GSI/分析阶段，两个项目仍在；诊断需重新读取且成功，双清单五项 fresh verification 再次通过 |
| 模型不可用时手动链 | 未配置模型密钥；上述确定性 GUI 链独立完成，没有成功模型回合，也没有发送真实原始资料到模型 |

原生保存框名称字段中的绝对路径被 macOS 当作文件名并转换为冒号，本次实际文件位于隔离工作区；磁盘检查定位到了真实保存文件，未把预期路径当作成功凭据。第二项目仍显示默认“新建内业任务”，并以唯一项目 ID 和清单区分，没有把未生效的自动输入算作改名成功。

两份草稿清单为 `manifest_d3e40780-c38f-440c-81d4-c26474e31d66`（GSI）、`manifest_8df3134c-cf25-44c5-bb63-49d9540be80e`（IN2）。六份成果的独立磁盘哈希见 [gui-output-integrity.json](./gui-output-integrity.json)。

## 留存与边界

真实工程原件、成果全文、诊断 JSON 与截图仅存本机 `/private/tmp/railwise-survey-e1708d7/evidence/` 及隔离工作区，不上传仓库。截图为原生 CUA 返回的 JPEG，文件扩展名已与编码一致。官网营销图仍为 94f1550 的中文浅色合成数据。

真实模型、历史用户数据迁移、专业结果签认、完整 locale/键盘/a11y/异常状态和本轮窄窗口仍未完成。原生保存框还有英文标题，不能宣称零英文残留。随后自由水准试算核和扫描规范来源增量不在 e1708d7 包内；须按其新提交单独验证。正式应用、用户配置、版本、Release、公开 feed 和官网部署未修改。
