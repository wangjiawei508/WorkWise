# 24a7edb 候选包实际验收记录

日期：2026-09-19；源码 `24a7edb9e8e1ad023f53be445d8aa7a204b8720e`；内部包版本 0.5.0；不是发布批准。

- DMG SHA-256：`65f3dee7326a4fec9fcfec20c1a61eb37580bf204879cb213d6dd6b47d7bc483`。
- ASAR SHA-256：`d89eb3c67dea9cd9b6c1c5c8f2cd4ec95e1d31dead9dbf9b6ef5b619318666a1`。
- Developer ID 签名严格校验通过。没有公证票据，stapler 返回 65。Gatekeeper 显示 Unnotarized Developer ID / security disabled，不计作公证通过。
- 安装于独立候选目录，正式应用和用户目录未修改；更新被禁用，未完成 updater 往返。

## 两种真实格式的 GUI 操作

COSA IN2：创建控制网任务 → 原生文件选择器导入 → 33 点、11 测站、160 观测 → 初始“需确认”且平差按钮禁用 → 质量校核 → 可平差 → 确定性计算 → 分析与精度 → DOCX/PDF/XLSX 预览 → 生成草稿清单。

GSI：创建水准网任务 → 显式填写源资料中的 6 个米制控制点 → 原生文件选择器导入 → 27 点、28 观测 → 质量校核 → 平差 → 分析与精度 → DOCX/PDF/XLSX 预览 → 生成草稿清单。

GSI 草稿 manifest：`manifest_90367847-87e4-479a-a63a-b1fcc31c614c`。
IN2 草稿 manifest：`manifest_55283de2-dbc2-4c23-b19d-83365d56b9f9`。
每个清单记录 3 份文件，均保持 Draft，未进行人工批准或签名。

同一已安装包的 Runtime 文件哈希和内容检查通过。IN2 对 OU2 最大坐标差 0.063416 mm；GSI 对独立 WLS 最大差 0.020316 mm。GSI 与历史 IN1 转换存在 7 段舍入/距离差异，需要专业人员判读，未自行签认。

## 实机发现，仍阻断最终验收

1. 成果已经生成，但成果页仍按监测 dataset 显示空状态。
2. 复核清单错误要求监测数据和趋势分析。
3. 返回设置后阶段重置，历史平差没有恢复为成果输入。
4. 校核通过后，统计卡仍显示“未校核”；原生格式也显示 JSON 专属提示。
5. 英文界面仍有 Runtime 中文诊断和待确认占位。
6. AI 输入框把缺少当前会话显示为连接 Runtime，尽管手动 Runtime 可用；本候选无模型密钥，尚无真实模型回合证据。

修复代码必须重新打包验收，本记录不能为后续提交背书。真实文件、坐标截图、AX 树和数值文件仅保留在本机候选 evidence 目录，不作为官网素材。
