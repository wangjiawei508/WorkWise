# 69e9728 候选：计算通过，重复建项使整体验收失败

源码 69e97289c04e822d2f01c2a68bdc0fa119164e23，隔离版本 0.5.0。Developer ID 深层严格签名通过，stapler 65（无公证）；Gatekeeper 的既有 override 不代替公证。

双真实 GSI/IN2 包内 Runtime 导入、校核、平差、DOCX/PDF/XLSX/manifest 与独立数值参考通过。两组只读复验五项通过，跨项目与篡改输出被拒绝，恢复原字节后通过。

GUI：合成 IN2 及真实 GSI 完整三格式成果链与草稿清单通过，两份清单的六份输出哈希/大小匹配。合成英文/中文复验五项通过，重启后进入内业恢复项目、阶段、结果和清单，再次复验通过。非法 COSA D.MMSS 分钟 60 在英文显示中保留英文原因、下一步与原始锚点，校核/平差锁定。中文浅色/英文深色、常规 1171×768、最大化 1490×768、点位表键盘 Right 滚动已检查。窄窗口拖动未生效，不计通过。

**阻断缺陷**：从侧栏新建工程测量项目触发无消费的 createRequestToken effect；项目改变令 createProject 回调改变，重复消费相同请求。退出前隔离数据库共 728 个项目。立即通过原生 UI 退出；保留全部隔离数据与截图，不涉及正式应用/用户数据。真实 IN2 的本包 GUI 链路尚未完成。后续修复必须在新精确候选重做验收，本包不作为合格候选。

代码回归证明单次事件重复触发、在途再次创建两种失败。修复改为事件直接调用加在途锁，离线事件不在重连后自动执行。测试证据与新候选结果另记。
