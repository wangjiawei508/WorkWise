# b9ea004 Private Quality Workflow Candidate

Candidate source: `b9ea004b668d68b34a7d82ece6674ef2273a9bc3`. Application version: `0.5.0`. [Private workflow 35954020211](https://github.com/wangjiawei508/WorkWise/actions/runs/35954020211) completed successfully on 2026-09-24.

This candidate includes the declared quality remediation workflow. **It does not include the subsequently requested new icon**, which belongs to a later source revision. This evidence must not be applied to that later package.

## Candidate Identity

- Bundle ID: `com.wangjiawei508.workwise.candidate.headb9ea004b668d`
- Expected target ZIP SHA-256: `db63b72181d41c2020fd1175201270fd43ac9b73dc8fded041bddfe01f32243b`
- Expected target ASAR SHA-256: `a8aa1bd0c4a07faf4b5903bd308faf9cd3c849a46e7014497f3ee5bcf49ac71c`
- Isolated candidate root: `<isolated-tmp>`

## Hosted Evidence

The authenticated private updater report records signature and stapled notarization verification, Gatekeeper enabled, one real private HTTPS manifest/ZIP transfer, installation and relaunch from an isolated same-source `0.0.0` probe to `0.5.0`, and preservation of the isolated data sentinel. This is not migration acceptance against historical user data. Public feeds and production releases were not modified.

## Local Results

Local installation completed with `ditto` into `<isolated-tmp>/Applications/RAILWISE AI Candidate b9ea004b668d.app`. Both target ZIP and installed ASAR matched the exact hosted report digests. `codesign --verify --deep --strict` and `xcrun stapler validate` passed. Local Gatekeeper is disabled; local `spctl` explicitly reported `override=security disabled`, so it is not evidence of acceptance with Gatekeeper enabled. The enabled-Gatekeeper evidence belongs to the hosted run.

`service-audit-002` passed using that application's own Electron 43.1.1 / Node 24.18.0 and packaged compiled services. It generated actual synthetic Survey leveling networks/adjustments, Engineering monitoring imports/analyses, and complete DOCX/PDF/XLSX/trend outputs; actual frozen plans and records were then used to create the four-event declared remediation chain. Exact idempotent retries, stale/cross-project refusal, unchanged-artifact correction refusal, single-byte corrected-source tamper refusal, restoration, service close/reopen, unchanged original retention records, and identical original business-table/file snapshots passed. Resolved declarations remain `deliveryApproval: not-granted`, and signature/conformity remain not evaluated.

`gui-seed-001` passed and prepared the Chinese-named synthetic project `合成整改链验收` in `<isolated-tmp>/workspace/synthetic-quality-workflow`. The candidate business store contains two complete draft deliveries with retained plans/records and **zero declared remediation workflows**. The GUI was not started by this audit. Exact navigation IDs are in `gui-seed-001-gui-instructions.json`; GUI interactions, user confirmation and theme/window acceptance remain separate work.

The original failed `service-audit-001` and its original script are retained. Its first assertion incorrectly required exactly three output files; the actual service also retained a trend image, producing four outputs. The correction checks presence of the required DOCX/PDF/XLSX media types while preserving extra members. The corrected script ran in a new directory; no failing dataset or report was overwritten, and no application source fix was necessary.

The initial `gh run` invocation without an explicit repository resolved an unrelated configured upstream and returned HTTP 404. The retry explicitly used `-R wangjiawei508/WorkWise`. The 589,611,966-byte combined artifact download was stopped because it was very slow. An authenticated HTTP Range probe returned 206 and enabled central-directory extraction of only the 296,397,665-byte compressed target ZIP entry (300,873,748 bytes inflated). Partial chunks are retained locally for resumable download; the target ZIP must pass the exact updater-report hash before installation.

## Evidence Files

- `installation-report.json`, `private-updater.json`, `native-updater.json`, `tls-preflight.json`: installation and real updater evidence. Private loopback feed URL is redacted in the archived copy.
- `range-download-receipt.json`: authenticated target-entry extraction and final target ZIP digest.
- `service-audit-001-audit-report.json`: preserved initial audit-script failure.
- `service-audit-002-audit-report.json`, `service-audit-002-workflow-receipts.json`, `service-audit-002-reopened-workflow.json`: exact packaged service execution and restart readback.
- `gui-seed-001-audit-report.json`, `gui-seed-001-saved-records.json`, `gui-seed-001-gui-instructions.json`: synthetic GUI fixtures and exact navigation identity.
- `evidence-index.json`: original and archived hashes and documented redactions. Large raw business snapshots remain in the private audit directory with hashes indexed here.

## Subsequent GUI Attempt

The parent task subsequently launched this isolated application and observed the English Code page. The native app selection call stalled for approximately 8,071 seconds; no remediation workflow UI actions were performed. The app was then normally quit through its application menu on 2026-09-24 at approximately 15:06 Asia/Shanghai, and its launcher exited with code 0. This attempt does not establish quality-workflow GUI acceptance. The later icon package is verified separately.

## Boundaries

All generated projects and observations are synthetic software acceptance inputs. Their record consistency and numerical checks are not professional signoff, standards conformity, production KPI evidence, or release approval. Packaged service scripts are not GUI interaction. No model credentials are needed or copied. Public release, tag, stable feed, and official download metadata are unchanged.
