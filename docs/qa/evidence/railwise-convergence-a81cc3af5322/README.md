# 收敛候选 a81cc3af5322 的实机发现

2026-09-19。本记录仅对应 `a81cc3af5322757373cb4637903649a3770afa64`，不是后续修复包的验收证据。

候选版本 0.5.0，从只读 DMG 安装到独立 Applications 目录。`codesign --verify --deep --strict` 通过，Developer ID Team `R35G7F4A9U`；Apple 公证凭据缺失，`stapler validate` 退出 65。Gatekeeper 当前环境返回成功不替代公证记录。更新禁用，无真实私有 feed，也没有 updater 往返通过记录。

## 已完成

- 已安装应用自带 Electron 执行包内 Runtime，分别完成 GSI 和 COSA IN2 导入、预检、确定性平差、DOCX/PDF/XLSX/manifest 内容及哈希核验。
- GSI 独立 NumPy 检查通过；与历史转换文件仍有七段末位/距离差异，须专业复核。IN2 与对应 OU2 精度比较通过。详细证据见旁列 JSON。
- 真实 GUI 中创建控制网任务、保存名称、切换中文和浅色、选择 IN2 文件、完成质量校核、运行平差、显示残差及精度结果。未借助 API 注入 GUI 数据。
- 没有 API Key 的隔离候选中，手动确定性功能可用；AI 实际问答未验收。

## 阻断与修复依据

1. 四阶段仍将专业任务导向旧监测导入/分析页。
2. 网络摘要误将两个源文件显式已知点当作全部 33 个网络点，校核后摘要未刷新。
3. 一次性平差未带观测期次，被错误判为只可审计；成果通知因此没有触发。观测期次应仅约束多期变形。
4. 控制点映射只有 Runtime 参数，GSI 无法在 GUI 补齐显式基准。
5. 常规窗口中内嵌 Survey 三列布局挤压残差表；专业摘要第十三项单独落行。
6. 设置帮助保留旧品牌，示例 JSON 含固定中文。
7. 英文模式下 Runtime 的中文诊断、旧基准占位与部分来源说明仍未完成 locale；不能宣称零中文残留。

前六项已进入后续源码修复与回归，必须另用新候选验收。截图与包含真实坐标的数值中间件仅保存于本机 `/private/tmp/railwise-survey-a81cc3a/evidence`，不作为公开营销截图。

本候选未完成 GUI 成果导出闭环、全面主题/窗口/a11y 验收、真实 AI 服务、专业签认、公证或 updater，不能宣布 P0 完成或发布。
