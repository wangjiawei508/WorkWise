# 规范依据目录与桌面接入验证

本增量为 GB/T 24356-2023 现有抽样和声明评分提供 9 个规则、16 个 profile 绑定的只读目录，保存精确版本、来源哈希、算法和条款/PDF页。通过 Runtime Bearer、严格 GET IPC 与 renderer client 关联既有结果；所有旧记录原样保留。目录解析不授予专业信任、标准符合性或人员签认，详见[方法及合同](../../SURVEY_STANDARD_BASIS_METHOD.md)。

## 整合检查

`c8af293` Runtime 与 `a4a40d7` 桌面整合后：

- 桌面全量 326 文件、2,714 项通过，2 文件/2 项跳过。
- Runtime 全量 179 文件、2,772 项通过，2 文件/22 项跳过；已有 opt-in 转换器进程测试不在默认全量中。
- 双端类型检查、完整构建通过；ESLint 0 error，1 条既有 Workbench Hook warning。
- 定向 Runtime 209 项、桌面 58 项通过。

全量日志是上述整合树的实际输出。独立复核随后发现单位评分式(6)条款定位缺失，`b59104a` 只修正新目录说明并增加回归；旧评分算法、旧 trace 和持久记录未改。修正后 Runtime 17 项和目录 DOM 13 项再次通过；不将修正前全量日志冒称修正后重跑。

公开归档的四份日志仅将本机绝对工作区前缀替换为 `<repo>`；原测试数量、版本、时间、构建结果和警告均未改动。独立探针中的固定鉴权身份及 localhost 地址只用于合成的内存路由验证，不是实际凭据或内部服务。

## 独立审查

[独立探针及结果](independent/README.md)在 `be0d7ac47550d4d178674f63ce4406ee7c614e66` 重新编译实际 Runtime、IPC schema、client 和组件：16 组解析、32 次只读路由调用、8 类错误身份拒绝及 7 项 DOM 状态检查通过。它使用 in-process Router 和合成身份，不是真实 HTTP/安装包 GUI；未读用户数据库或凭据。

原 P2 问题是新目录未列 `6.2.4.5`，却声明覆盖单位评分式(6)。保留的官方 PDF 第10/11页经实际查看确认：式(6)属于 `6.2.4.5`，`6.2.5` 是等级与表3。修正后的新目录写清历史 trace 标签偏差，旧记录不被静默修正。最终独立复核没有发现未解决的 P1/P2 问题。图像摘要与初始探针修正记录保存在独立说明中；不打包标准全文。

精确签名安装包、原生 PDF 打开、主题/尺寸/键盘以及用户确认另行执行；本目录只作为源代码验证证据，不能勾选整体产品验收。
