# 94f1550 精确候选实机复验

2026-09-19，源码 `94f1550a794643f69f259027c6a6a889a18b02f6`，安装包版本 `0.5.0`，bundle `com.wangjiawei508.workwise.candidate.head94f1550a7946`。应用位于隔离目录 `/private/tmp/railwise-survey-94f1550/Applications/RAILWISE AI Candidate 94f1550a7946.app`。本轮重新建立独立候选数据目录，没有修改正式应用或正式用户配置。

## 包身份

- Actions 公证候选运行：35434481062。
- DMG SHA-256：`8d2d70424259f9d85a2f1ca61cb0e30f6de6bd76bd68c9cec99409574bed8f04`。
- app.asar SHA-256：`6eaeb230fe2fd2cca7474a9362e7dfe0b5ea17bf391f4818180e719787381fbf`。
- 既有本地记录中 codesign / stapler / spctl 均退出 0。spctl 同时显示 `override=security disabled`；这不等同于开启 Gatekeeper 的独立主机验收，签名和公证票据检查仍各自有效。

## 本轮 GUI 记录

1. 用 Computer Use 直接选择精确候选应用，切换中文、浅色；空项目页显示可创建任务，侧栏一次点击产生一个项目。
2. 创建“控制网内业演示（合成数据）”，保存项目上下文；从原生文件选择器导入仓库 `golden-plane-control-e2e.in2`，显示 4 点、1 测站、5 观测，源 SHA-256 为 `4281cc7a10673570756867e8c8665f22f82611839bd34c396a5eba9dbb89d9f0`。
3. 校核前平差禁用；校核通过后运行 `workwise-survey-adjustment-7`。最大点位中误差 `1.35922e-7 m`、水平残差范数 `1.37267e-7 m`；点位页显示未知点的 XY 标准误差椭圆解释。
4. 生成 DOCX、PDF、XLSX，再生成 `manifest_18cb3312-0c8b-4f3d-b623-3f957526971f`。清单保持草案，未成为批准交付。
5. 在中文浅色界面执行“重新验证成果”：清单文件与持久记录、输出哈希和大小、输入绑定、严格 Survey 重算、来源完整性与准入五项均通过。
6. 切换英文深色后仍为一个项目。仅给本次合成草稿 DOCX 追加测试字节：界面明确显示输出文件 hash 不匹配，其余四项通过。恢复原始字节后重新执行，五项均通过且仍为 Draft。
7. 检查英文深色常规和最大化窗口，审查清单与复验结果可读。用户命名的项目和历史会话标题保留中文，不自动改写用户文本。
8. 恢复中文浅色并检查最大化布局；退出并重启同一包，重新进入“内业”后恢复同一项目、成果阶段和原草稿清单。项目数量仍为 1，重新验证五项通过。

公开营销图只使用合成任务：中文浅色平差、成果中心、模型设置三张均在本包重新截取。设置页内容未变，因此重拍的模型设置图与旧图字节哈希相同。见[逐图哈希清单](../../../../website/products/screenshots/workwise/candidate-screenshots.json)。

## 边界

本轮闭合 OpenSpec 任务 56 的精确包只读控件验收，不闭合专业复核、数字签名、规范符合性或全量 UI 验收。当前候选没有配置模型 Key；真实模型会话、双真实格式完整 GUI、窄窗口/全键盘、私有 updater 往返和用户本人确认仍需继续。

本轮新增统计诊断、对外 MCP 适配和“无网络时摘要读取项目基准”修复不在该包内。代码测试和本包 GUI 证据分别记录，不交叉替代。
