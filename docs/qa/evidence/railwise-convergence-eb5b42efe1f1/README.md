# eb5b42e 候选验收

源码 eb5b42efe1f1076be7b008b27a5c9cc42c6faf7c，0.5.0 隔离候选。Developer ID 严格深层签名通过。stapler 65，无公证票据；Gatekeeper 的既有 security-disabled override 不算公证。

双真实 GSI/IN2 包内 Runtime 导入、校核、平差、三格式导出、草稿清单及独立对照通过。GUI 使用合成 IN2，完成同一链路；重选当前任务会话后摘要、结果选择及预览均保留。切换设置后预览内存状态重新加载为历史清单，未将其误计为当前会话保留。合成清单三份文件哈希通过。

中文浅色实机发现点位表第一列将追问按钮挤成竖排，后续源码增加表格最小宽度、横向键盘滚动容器与按钮不换行；本包仍保留缺陷。新增成果复验功能不在本包中。

本包 GUI 尚未完成双真实格式、完整主题/尺寸/键盘验收；模型配置显示未配置密钥，不重复提交注定失败的模型请求。此前真实模型 401 记录仍有效。公证、私有 updater 和专业确认未完成。
