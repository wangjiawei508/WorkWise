# 监测复算源码检查

产品源码：`b193fc70d6363dc768e715087ca9c0d9975158c9`。本档案记录原件驱动的监测复算源码检查，不代表安装包、真人确认或公开发布。

| 检查 | 实际结果 |
| --- | --- |
| 桌面全量 | 2,826通过、2跳过 |
| Runtime最终全量 | 2,895通过、22跳过；包含解压资源预算与同刻排序修复 |
| Runtime较早全量 | 2,887通过、22跳过；保留为后续补丁之前快照，不冒充最终计数 |
| 双端类型、构建 | 通过 |
| lint | 无错误；1条既有Workbench Hook warning；后端最终增量lint无输出 |
| strict OpenSpec | 11通过、0失败 |
| 独立旧新差分 | 1,000组合成输入、6个直接边界通过 |

`logs/`保留命令原始输出，包含本机路径和测试进程ID；无真实工程资料或凭据。构建日志为本地未签名编译，不能代替后续签名候选证据。桌面日志的Node localStorage ExperimentalWarning未影响退出码。

`independent-review/`保留独立审查的原脚本、结果及说明，原运行在提交前HEAD `59d2d56` 上执行。脚本用当时HEAD提取旧函数，冻结后不可直接运行其原命令来复现；如需再次对比，应显式从上述历史提交读取旧函数并对照产品源码。1,000组差分只证明提取保持原行为，不是独立工程数学oracle；13项有理数基准另见[复算合同](../../RAILWISE_MONITORING_REPLAY_ACCEPTANCE.md)。

审查发现并已修正：系统numeric collation会改变同刻ASCII ID次序；资源测试中的无效列号可能掩盖其它预算失效。流暂停独立probe证明选定输入没有后续data/未捕获错误；JSZip/pako可能已将当前压缩块的额外数据放进可读缓冲，因此32 MiB是解析读取预算，不是进程内存硬上限。

新统计22项及旧统计37项通过由执行台账记录；此档案不虚构未捕获的测试日志。当前精确包进展以[执行台账](../../RAILWISE_SURVEY_CONVERGENCE_STATUS.md)为准。

云端推送质量检查35537092201通过。PR质量检查35537095479第一次的Windows插件测试两项超过5秒：配置工作区目录解析和按prepared ID安装；其余两个作业通过。本轮没有修改这两项实现/测试；仅重跑失败作业，attempt 2全部通过。`logs/railwise-monitoring-replay-pr-ci-failed.log`保留第一次实际失败输出，`pr-ci-attempt2.json`与`push-ci.json`保留最终作业状态。重跑通过不证明超时根因已确定或修复。
