# 6ff82c8 精确候选实机验收

本记录只覆盖源码 `6ff82c8c27466ee42f2604b053397fd132d5a0a7` 的 macOS arm64 隔离 `0.5.0` 候选，整体状态为 **partial**。同包的 13 条合成 GUI 试算、双真实 P0 成果链及重启检查已完成；用户本人确认、真实模型成功回合和专业来源复核仍未闭合。没有公开发布。

后续 `7a01de3` 的“声明检查关联评估”不在本包内，不得借用本报告证明该功能已完成安装包验收。

## 包与升级身份

- Bundle ID：`com.wangjiawei508.workwise.candidate.head6ff82c8c2746`。
- ASAR SHA-256：`ba7edf57c3afe50473a633700a96898c773a4ab42b255eb42c9da91e2e3ebe4d`。
- 应用 ZIP SHA-256：`57885b6b8e96182a34c44b2b7faba6aabca2cfd6d5cb518b1c776982c2d8149d`。

`private-updater.json` 记录真实私有 HTTPS 下载、原生安装、目标重启与数据哨兵保留。本机安装使用同一应用 ZIP，ASAR 与云端实际更新后的 ASAR 一致。应用 ZIP 通过外层附件 HTTP Range 提取；未重算完整外层附件摘要，不把应用 ZIP 校验扩大为外层附件校验。

本机 `codesign --verify --deep --strict`、`stapler validate` 通过。虽然 `spctl --assess` 返回 0，本机 `spctl --status` 为 disabled；启用状态的 Gatekeeper 验证来自云端私有 updater。未改变本机或生产信任配置。同源码 `0.0.0 → 0.5.0` 是隔离 updater 探针，不等于真实旧用户迁移；报告中的 `frontier` 是隔离探针通道，不是公开 feed 推广。

## 13 条真实 GUI 合成试算

这些记录由安装包 GUI 实际创建，保存在同一隔离候选的合成项目中。独立核验器只读 SQLite，不调用产品计算器。

| 范围 | 实际记录数 | 独立结果 |
| --- | ---: | --- |
| 两历元参考 | 3 | GLS、奇异 GLS 拒绝、显式等权参考均符合预期 |
| 静态观测追加 | 3 | 普通追加、旧基线拒绝、256 观测/16 参数最大模型；共 130 次追加前缀 SSE 核对 |
| 统计检验族与 Huber | 3 | 四成员家族保留缺失分母、严格内点 Huber、平坦解 Huber |
| 声明质量评分 | 4 | 完整评分、子项否决、待检、多精度项恰 60 不可用 |

完整单位评分精确为 `9141/100`；否决与未解决案例不暴露被接受的综合分。最大静态模型的 128 条追加前缀由独立有理数拟合核对。Huber 不宣称已认证唯一性或协方差，统计族不因缺失成员缩小分母。

参考与 Huber/家族核验器原先选择项目内全部试算；在混合项目上改为按 kind 只读筛选，再调用原核验函数。`independent-reference.json` 保留首次未筛选失败及后续成功，未删除失败记录。其算术与原始绑定核对不冒称 JavaScript canonical hash 的独立逐位重放；质量评分核验器的整数/字符串存储摘要校验另有明确记录。

## 保存、重启与损坏恢复

原生 Save As 取消未宣称保存；实际静态最大模型导出与数据库完整记录一致，字节严格等于 `data_json` 加一个换行。文件 965,532 字节，SHA-256 为 `9ddd615c28a58d4f1043cce605e44cb3336e9f55a202631157685d7c55558dea`，保留 256 条观测、16 参数和 128 步。

`quality-corruption-execution.json` 记录实际注入/恢复返回值：直接 UPDATE 被 append-only trigger 拒绝后，仅对固定合成评分的 `declaration_bytes` 增加一个 ASCII 空格，其余行、字段和摘要不变；单事务内恢复原 trigger SQL。GUI 复验拒绝损坏记录、健康历史仍可恢复。随后恢复全部原字节，GUI 恢复成功，独立四条评分再核对通过。`quality-corruption-preparation.json` 的 `prepared-not-injected` 是先前准备时点，不替代实际执行报告。

完整退出后在相同隔离环境重启，原高级模型与评分记录可恢复。AX 可能捕获异步加载中的瞬间，需结合独立数据库、导出报告和操作记录，不能单凭某一 AX 声称全部交互完成。

## 双真实 P0 与重启

`p0-summary.json` 只归档聚合结果：3 项目、2 个 revision-2 网络、2 次算法 `workwise-survey-adjustment-7` 平差、2 个 `draft` 清单。六份 DOCX/PDF/XLSX 文件的哈希与长度、两份保留源字节、所有原始锚点范围与观测关联均核对通过。

IN2 为 33 点、160 观测、174 锚点、87 自由度。独立读取原 COSA OU2 比较 33 点，印刷舍入与精度包络不符数均为 0；最大平面差约 0.0634158392 mm，最大归一化差约 0.088999252 sigma。

GSI 为 27 点、28 观测、156 锚点、7 自由度。独立 NumPy 与原参考 IN1 比较 7 个未知端点及 6 个控制点，最大高程差约 0.0203155093 mm；另按原始十进制 GSI 重建 28 条观测，同源 21 个未知点解与包结果最大差约 1.7053e-10 mm。**原参考 14 条路线中仍有 7 条高差超过半个印刷单位，历史转换来源未闭合**；数值符合精度包络不等于转换来源或专业验收已通过。

完整退出、确认无候选进程并重启后，GUI 对两个原清单再次执行五项复验。持久审计共 4 次成功，每次清单/输出/输入/Survey 重放/来源五项通过、绑定稳定、存储摘要有效；项目数仍为 3。复用了历史检查逻辑，核验的是本包新记录，没有沿用旧包结果。

## 界面与剩余门禁

中文浅色、英文深色、常规和近最小布局已有截图。常规图为 1171×768 像素，近最小图为 963×641 像素，最后中文浅色参考结果图为 1490×768 像素。图片尺寸不证明原生窗口或 CSS viewport 尺寸；精确支持下限仍未独立量得。统计族表格在近最小图中可键盘聚焦并横向查看临界区间；不据此声称完整键盘、全部状态或无障碍验收完成。结束时恢复中文浅色合成参考页。

用户本人对精确版本 UI/功能的确认、专业解释与 GSI 来源复核、真实模型成功回合（含 V4.1 官方搜索）、真实旧用户迁移、完整 locale/状态/a11y 仍未闭合。两份候选数据不构成代表性生产指标。本报告不授予发布批准，也不把哈希、试算或草稿视为签认后的正式交付。

## 归档边界

`gui-acceptance.json` 为最终归档索引；`gui-acceptance.source.json` 保留定稿来源记录的原字节，其中历史 `draft-partial` 标记原样保留。`package.json` 中 `guiAcceptance: not-tested` 是安装后、GUI 操作前的原始快照，后续证据由本报告及最终索引承接，不回写原日志。

编号 AX/截图只含合成内容；最后两张图侧栏仅有自建通用验收名称，无真实项目名称、原始坐标或文件内容。真实 P0 AX、输入、含坐标的数值明细、真实成果、私有路径脚本及启动日志均不上传。`private-updater.json` 经检查无 token 后按原字节复制，`native-updater.redacted.json` 仅隐藏完整 feedUrl；原始私有报告留本机。`manifest-hashes.json` 列出本目录除自身外每份文件的 SHA-256，复制证据原始字节不变。
