# 导入事件与转换器边界落地

2026-09-20，整合源码至 `22589db`。这些代码不在 `281dc87` 安装包内，尚未做新包 GUI。

转换器按 stdout/stderr 各 1 MiB 实际字节数拒绝超限，超时和本地 AbortSignal 取消时杀进程组并等待 close，父进程成功退出也回收同组后台进程。结束后检查普通文件及大小，注册器用 O_NOFOLLOW/O_NONBLOCK 与分块有界读取拒绝符号链接及超大输出；原始来源保留。AbortSignal 只接至本地 ingest/convert/executor，不声称桌面取消已接通。COSA NET 新增 8 MiB、16384 字节行长、10000 记录/坐标上限，OU1 比较入口先核对维度。

主树实际 macOS sandbox 36/36 通过，包含真实父子进程回收、UTF-8 输出、4096/4097 输出边界、取消、符号链接及网络拒绝。九类格式使用固定哈希的合成 cp adapter，不是厂商私有转换器验收。主树三文件格式回归 156/156；独立实现树另跑相邻七文件 241/241。独立树首次测试用的 1 秒超时未等到 PID 文件生成，测试预算改 3 秒后通过，产品限额未放宽。

新增导入事件表保存 started、committed、finished，不改原网络及历史证据。committed 与网络、原始账本和幂等键同一事务；解析/HTTP 拒绝、重放、侧写失败、审计不可用和未完成请求分别保留。审计写入失败不会报告成功；正文读取中的进程崩溃和审计存储本身不可用无法凭空枚举。只读 Python 报告校验事件摘要、身份、时间和生命周期，只统计已记录的首个文件键子集，不把它命名为产品总体首导率或源文件验收通过率。

主树服务与审计 58/58、Python 26/26 通过；独立实现树含路由另有 66/66。真实 SQLite 到 Python CLI 用例核对首次失败不被重试覆盖、未知请求计数、生产代表性未测和无数据库改写。完整生产指标仍依赖有定义的真实总体、有效成果及专业批准证据。

整合全量：桌面 2699 通过/2 跳过，Runtime 2755 通过/22 跳过；默认 Runtime 跳过的实际沙箱项目已在上方显式命令独立执行。双端类型、构建、strict OpenSpec 11/11 通过，lint 0 error/1 既有 Workbench Hook warning。首轮 Runtime 三个旧 auto 模型预期失败，原日志保留；更新为新分类器/回退 ID 后全量通过，显式旧模型兼容用例保留。

GitHub `7431ce1` 两轮 Quality（35486609920、35486607812）失败于抽样界面的旧能力边界断言；新文案已反映实际存在的评分与关联评估，而旧断言仍要求“评分未实施”。更新双语边界断言后，包含该用例的桌面全量通过。旧远端失败日志保存在本机 `/private/tmp/railwise-survey-281dc87/quality-35486609920-failed.log`；远端后续检查单独记录，不用本机通过代替 CI。
