# Candidate 53213 Resume Audit Method

This is an unexecuted input template and audit procedure, not a candidate acceptance result. The candidate head is `53213de739d0b1f3f173f883dcdac21cdc4fdfc9` and the isolated root is `/private/tmp/railwise-survey-53213de`. No credential files are needed by this audit. Head/root are configuration values, not hardcoded script constants.

1. Retain the signed package identity and the independently measured installed ASAR hash. Fill all placeholder fields in `53213-input.template.json` using the exact GUI-selected project, network, thread and plan. Record the original Task and mock Turn IDs before continuing; do not replace the Task ID with a newly created task.
2. After the mock phase, verify in the real candidate that the original Task is stalled, the Turn is failed specifically with `engineering_plan_steps_incomplete`, all seven exact mock replies remain in the transcript, and there are zero tool calls, successful step receipts and adjustments. Stop the mock, retain its report, and independently verify its local-token file is absent. This pre-resume check and its screenshot remain separate acceptance evidence.
3. Switch the same selected provider to the temporary real relay and use the GUI typed-resume action. Record selected provider/model/effort and the new successful Turn ID. Preserve the failed mock Turn; no transcript edits or replacement Task are permitted. Retain the successful tool/result records, actual adjustment/delivery IDs, screenshots and stopped relay report.
4. Fill `expected.previousFailedTurnIds`, `expected.turnId`, `mockReportPath` and `relayReportPath`. Use `strictExecutionMarkers:true`. `mockAssistantTextCount:7` is the expected negative-probe count, while the actual report count is calculated and compared. The mock and resumed Turn must retain the same selected provider/model/effort for this specific scenario.
5. With the candidate idle, run `node audit-candidate-execution.mjs INPUT.json` and retain stdout as the before-restart JSON. The audit opens SQLite only on its private copies, detects source changes and fails if source DB/WAL bytes move during capture or audit. It reads only explicitly scoped candidate records and input evidence.
6. Restart the same installed candidate normally, reopen the same plan in the GUI and retain the screenshot. Change only `phase` to `after-restart`, then run `node audit-candidate-execution.mjs AFTER_INPUT.json --compare BEFORE_REPORT.json`. The package, original/resumed identities, original failed Turn and transcript hashes, Task events, successful result/receipt hashes, and all three output-file hashes must remain unchanged.

The four step receipts do not contain a Turn ID column. Their origin is corroborated independently by zero tool execution in the retained failed Turns, exact successful tool calls/results in the resumed Turn, and matching receipt parameters/handles. The report states this limitation explicitly. A screenshot and pre-resume record remain necessary to establish that the GUI action and zero-receipt state were actually observed before continuing.

An optional independent numerical oracle may be supplied through `referenceResultPath`. Without it, the audit compares the full exported deterministic result against stored records and clearly does not claim independent numerical recomputation. The old fbb audit remains a historical self-check only; it cannot satisfy strict new-candidate requirements.
