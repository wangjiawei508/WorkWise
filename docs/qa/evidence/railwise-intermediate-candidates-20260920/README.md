# 2026-09-20 中间候选证据

本目录用于保留中间候选的身份与有限验收结果，不代表最终候选通过，也不构成发布批准。各候选的结果只适用于其精确源码提交。

- `267aade`：完成私有云端更新往返、完整附件校验及本机隔离安装。没有启动 GUI，没有复制或读取用户数据，没有用户验收。
- `27738f4`：私有云端签名、公证及真实更新往返通过；本机只保留已验证完整 SHA-256 的云端证据与目标附件元数据，不下载目标大包、不安装、不启动 GUI。

`candidate-summary.json` 由 `archive-evidence.mjs` 从经核验的本机记录白名单提取。它保留精确 HEAD、工作流 ID、哈希、检查状态与下载故障恢复事实，不包含候选环境文件、密钥、私有 feed URL 或原始日志。`source-provenance.json` 记录原记录与归档摘要的 SHA-256，仅供来源核对；来源哈希本身不等同于公开签名证明。

267 下载最初失败：563 个分块中有 2 个长度不足（第 464、478 块），其余 561 块保留。仅补传这两个短块后，重新校验了完整 589,380,945 字节附件的 GitHub SHA-256 和 ZIP CRC，再校验内层应用 ZIP 及 ASAR。本机签名、公证票据与 `spctl --assess` 检查通过；本机 Gatekeeper 状态为 `assessments disabled`，云端为 `assessments enabled`，两者不能混称。

所有源记录均明确没有公开发布操作。后续修复后的最终候选仍须单独完成完整安装包、界面、功能与真实更新验收，不能继承本目录中的 GUI 或用户批准状态。

运行 `node docs/qa/evidence/railwise-intermediate-candidates-20260920/verify-archive.mjs` 校验归档完整性与验收边界。归档生成脚本仅在来源记录齐备时运行，使用排他创建防止覆盖既有证据。
