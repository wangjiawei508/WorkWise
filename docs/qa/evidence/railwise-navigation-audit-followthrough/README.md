# 作用域、精确导航与复验生命周期源码验证

日期：2026-09-20。归档时 HEAD 为 `612c4783740bfd83b4023b4a9dcd85e5880338ed`，变更尚未提交。[机器记录](source-verification.json)保存当前 24 个源码、测试、方法和页面观察文件的 SHA-256；这是当前工作树快照绑定，不冒称每次较早测试都使用了完全相同字节。

本轮范围：工程读取/写入作用域隔离、一次性精确证据导航、缺资料追问、品牌及语言修正、复验 started/finished 持久审计，以及规范表格的精确页面链接。旧结果、审查状态和历史 trace 不被回填。规范映射实证另见[逐页观察](../railwise-standard-basis-20260920/page-review.json)。

## 测试及时间边界

| 检查 | 实际结果 | 证据和边界 |
| --- | --- | --- |
| 桌面整合全量 | 327 文件、2,752 项通过；2 文件、2 项跳过 | [原日志](desktop-final-tests.log)，13:09:29，35.80秒。已包含页码源码细分和创建/列表竞态修复；尚未包含随后新增的两项 profile 链接 DOM 用例。 |
| 规范依据 DOM 定向 | 1 文件、15 项通过 | 13:12:41，0.802秒，会话结果摘要。其中两项是全量之后新增；实际目录验证平面 61/62/63/60 与高程 64/65/66/64 的表页和引导页顺序及 openExternal 调用。不能将15项叠加成新的全量总数。 |
| Runtime 全量 | 179 文件、2,780 项通过；2 文件、22 项跳过 | 12:42:54，22.25秒，会话结果摘要。已包含复验生命周期；早于本轮表页精细化，不声称在该修正后重跑完整 Runtime。 |
| Runtime 目录/评分/抽样定向 | 3 文件、164 项通过 | 13:04:31，0.512秒，会话结果摘要。包含全部9规则的独立页观察映射断言；命令中额外写入的路由测试路径不存在，未执行单独路由测试。 |
| Python 指标 | 37 项通过 | 会话结果摘要；开始分母、期末未完成、跨期、历史兼容、损坏拒绝与旧统计回归。 |
| 独立导航复核 | 4 文件、92 项通过 | algorithm_review 的实际会话结果，13:08:44，2.03秒；没有原始磁盘日志。 |
| 独立复验审计复核 | 14 项通过 | interface_review 的实际会话结果摘要；不代表14个真实生产项目。 |

上述没有原始日志的检查明确记为会话结果摘要，不拼造终端日志。确切命令、归属、计数和限制写入机器记录。首次目录摘要断言仍使用旧摘要而失败，更新为新目录的精确摘要后相关164项通过；该失败是已声明定位调整后的预期快照更新，不是标准数值算法改变。

## 最终检查

归档前重新执行：[桌面类型](desktop-typecheck.log)、[Runtime 类型](runtime-typecheck.log)、[OpenSpec 严格校验](openspec-strict.log) 11/11、[17个相关 TS/TSX 文件 lint](related-lint.log)，全部退出0。本次未再次执行完整测试。较早[全量 lint](lint.log)为0 error、1条既有 Workbench Hook warning。

[最终构建日志](final-build.log)保留 Runtime 编译与 Electron Vite 构建；主任务已实际取得该构建进程退出0。构建日志保留既有动态/静态导入警告。它是源码构建，未因此产生新的签名、公证、安装包或 updater 验收结论。

原始桌面测试、构建和全量 lint 日志只将本机工作区绝对前缀替换为 `<repo>`；结果、数量、时长和警告不变。机器记录保存脱敏前后摘要，公开文件不含本机用户名、凭据或真实工程数据。最终检查日志是本次实际工具输出，加上命令和退出码。

## 独立核查与安装包边界

独立审查复现并修正延迟写入串项目、旧定位请求重放及新建/旧列表响应竞态。复验审计覆盖开始/结束/原终态写入故障、真实子进程 SIGKILL、跨期和旧表兼容；它证明已记录服务调用的测量能力，不证明生产总体覆盖或 KPI 达标。

algorithm_review 实际逐张查看 PDF 60–66 的磁盘扫描并核对 PDF/PNG 摘要，确认表43@61、表44@62–63、表45@64、表46@65–66及引导60/64；未发现确定缺陷。原来源研究保留整节范围，不冒称每张表都出现在范围首页。

[`be0d7ac47550` 精确包证据](../railwise-convergence-be0d7ac47550/README.md)独立保存。这些源码增量不在该包中；该包仍打开 PDF60 的条款引导页。本归档不能覆盖该历史包，也不能勾选新包主题/尺寸/键盘、正式用户确认、专业签认或公开发布门禁。
