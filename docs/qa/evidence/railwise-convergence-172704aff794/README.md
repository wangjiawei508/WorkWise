# 172704a 签名候选验收

2026-09-19，精确源码 `172704aff7948775bd5e24ad25b69c09200b4a19`，隔离候选 0.5.0，未公开发布。包身份及检查范围见 [acceptance.json](./acceptance.json)。

- 已完成独立安装及 Developer ID 严格深层签名验证。公证无票据，stapler 返回 65；Gatekeeper 的既有 security-disabled override 不能作为公证验收。
- 已安装包的 GSI、IN2 真实资料 Runtime 导入、校核、平差、DOCX/PDF/XLSX 和独立参考比较通过。GSI 最大差 0.020316 mm，IN2 最大差 0.063416 mm。GSI 来源差异仍需专业复核。本包没有重跑双真实 GUI 链路。
- 原生文件选择器导入合成 IN2，完成校核、平差、成果预览和待审查 manifest；三份正式清单所引用的文件磁盘哈希一致，详见 [gui-artifact-hashes.json](./gui-artifact-hashes.json)。成果仍为 draft。
- 预检、残差、输出文件、历史清单的一键追问准备通过。手写问题保留，选择的引用跨阶段和设置页面保留。残差定位至第十条记录、字节 129 起 11 字节，原文 `C,S,100.000`。
- 英文深色、中文浅色、最大化与窄窗口均目视检查。窄窗口确实切换为上下布局；截图像素尺寸不等于窗口逻辑尺寸，未宣称覆盖精确最小宽度。对话输入到更多操作、模型设置、发送的 Tab 顺序通过，不能替代全键盘/屏幕阅读器验收。
- 退出并重启后，中文浅色、任务、成果阶段、manifest 及失败咨询历史恢复。
- 一次合成清单咨询返回 401 Authentication Fails (governor)。界面显示配置/重试入口，没有得到模型回答。提交内容在历史中保留，输入草稿和引用已清空；这不是成功模型验收，也不是发送失败保留草稿的证据。
- 实机发现总览侧栏仍将草稿统计为“已归档”、控制网仍显示监测流程。后续源码已修复并回归；不能将此包记为该修复通过。

公证、真实私有 updater、成功模型回合、真实旧数据迁移、完整主题/键盘矩阵、Dock/托盘及用户专业确认仍待完成。官网图片保持此前 b8df979 的明确来源，本包错误状态截图仅用作内部验收。
