# 质量证据工作区与规范抽样增量

本模块是技术证据保全，不是 GB/T 24356-2023 完整质量验收。它不认证人员、不评分、不签字、不批准成果，也不代替 Survey 数值重算和源文件准入检查。原有复验功能继续单独提供后两项能力。实施依据及剩余生产层次见[接入审计](RAILWISE_SURVEY_QUALITY_INTEGRATION_AUDIT.md)。

## 证据工作区

在待审清单旁展开证据保全，阅读本次成果文件及各自摘要后明确确认，冻结不可修改的计划。计划保存项目修订、manifest、实际文件集合及每项要求对应的确切成员；不会把另一个文件当成已要求的文件。每次后续计划都是新记录，不静默修改历史。

用户另行创建检查记录，按项执行字节完整性检查或留存指定成员。服务端决定检查结果和system身份，请求不能填写通过状态、真人身份或删减必检列表。所有输出文件及原始manifest保留，独立质检数据库只追加证据。

- 本地认证、项目/清单/计划/成果包/成员绑定。
- SQLite 追加保护、同链尾比较、载荷绑定幂等；读取重核行身份、链、原文件和保留字节。
- 失效或损坏的历史条目显示具体不可恢复状态，不阻断同页有效条目；原详情仍严格拒绝。
- 文件限定在本项目成果目录，拒绝符号链接、特殊文件、读时替换、越界及超限；每文件32MiB、每包128MiB。同步磁盘读取具有字节上限，不声称具有墙钟超时保证。
- 本地链和摘要不等于独立保管；`coverageStatus`、`standardConformity`、`humanSignatureVerification` 仍为 `not-evaluated`。

认证API位于 `/v1/engineering/projects/:projectId/quality-plans`、`quality-evidence`、`quality-records` 及其详情/checks/verify。桌面 IPC 同样限制方法、请求合同与分页。旧 monitoring 质量页保留，本增量不改已有数据或 reviewStatus。

## 独立抽样纯内核

[抽样内核](../../kun/src/engineering/survey-quality-sampling.ts)绑定官方 GB/T 24356-2023 PDF、表1页面、完整16区间及其哈希。它显式接收单位成果定义、总体、阶段、检查方式与随机来源，禁止从原始观测数推断单位成果。过程检查及最终内业强制全数；允许抽样时执行表1与1001及以上的最少均匀分批规则。

无放回选择采用有版本的 HMAC-SHA256、uint32拒绝取模偏差和部分Fisher-Yates，保留种子声明、总体顺序及完整结果哈希。这是可复现的软件选择，不是规范指定算法，也不认证种子随机性、空间均匀性、总体完整性或前轮检查。数学精度恰为20的规范歧义、完整错漏评分、人员授权、独立签认及交付批准不在此增量内。抽样纯核后的工作区接入见下一节；材料检查、评分与签认仍是独立未完成范围。

## 冻结总体与首轮抽样工作区（后续源码增量）

工程任务中可以单独展开抽样工作区，填写成果类型、单位成果类型、定义说明及每行一个的有序单位 ID。定义按实际 UTF-8 字节留存并绑定哈希，标记为用户声明，不冒充规范原文或专业核定。不能从观测数、文件数或清单数量自动推定单位成果。

冻结后显式选择过程检查、最终内业、最终外业或验收。前两阶段只允许全数；后两阶段可选全数或表1抽样。当前只支持首轮，每总体每阶段至多一份运行。随机种子由 Runtime 在保存运行时产生，同键同载荷重试返回原记录；换幂等键也不能在同阶段重抽。重新创建总体仍可能产生另一个样本，因此不宣称独立随机见证或防选样操纵。

- 1 MiB 请求、最多10,000个唯一单位、64 KiB定义原始字节；超限拒绝，不截断。详情仅摘要，单位/样本分页最多100项。
- 独立追加SQLite数据库；读时校核当前项目修订、SQL/JSON身份、定义字节、总体顺序、来源、算法、完整样本重算及哈希。
- 历史损坏项单独标明；工作区切换项目、修订或离线时清空旧结果并丢弃迟到响应。模型离线时手动流程可用。
- 认证API：项目下 `sampling-populations`、详情及`units`；`sampling-runs`、详情及`samples`/`verify`。桌面IPC同样严格验证方法、载荷、分页与原始字节上限。

本节冻结源码桌面全量2,606通过/2跳过、Runtime1,852通过/3跳过，双端类型、构建、strict OpenSpec11/11通过；lint0错误/1既有warning。独立Python真实服务存储复算及篡改负例见[接入证据](evidence/railwise-sampling-integration/README.md)。本节源码不在ba38649安装包中；新精确包验证结果须另行记录。选择样本不是材料验收、规范评分或真人签字，既有成果审查状态不会改变。

## 验证边界

前一精确包 [4f859c0](evidence/railwise-convergence-4f859c081b33/README.md)不包含本增量。本增量的[ba38649精确包记录](evidence/railwise-convergence-ba3864917e12/README.md)现已完成质量工作区限定GUI、重启、篡改和窄窗检查，范围与尚未验收项分别记录。

冻结源码自动化结果（2026-09-20）：桌面全量2584通过/2跳过，Runtime全量1825通过/3跳过；双端TypeScript、构建、strict OpenSpec 11/11通过，lint无错误、有1项既有Workbench Hook警告。本增量新包限定GUI证据见上述记录，用户亲自确认尚未完成。
