/**
 * Compatibility boundary between the WorkWise desktop and its bundled runtime.
 *
 * Increment this value whenever a desktop release requires request fields,
 * response fields, or tools that older runtimes cannot safely understand.
 */
export const WORKWISE_RUNTIME_PROTOCOL_VERSION = 1
