# Changelog

WorkWise 的正式版本变更记录。各版本的完整说明同时保存在 `release/` 目录。

## [0.4.2] - 2026-08-27

### Added

- 支持 JPEG、PNG、GIF 和 WebP 图片输入；`auto` 模式仅在当前使用官方 DeepSeek 地址，或当前 Provider 明确配置或发现 `deepseek-v4-flash-vision-exp` 时，为当前回合选择该视觉模型。

### Fixed

- 修复侧栏多个同名空壳线程遮挡真实历史会话的问题；确定无消息的非当前空壳不再展示，有消息的历史会话可恢复原消息。
- 修复视觉模型不可用或读取模型设置失败时编辑器内容可能被过早清空的问题；未发送的文字和附件现在保持原样。

### Compatibility

- 不删除或改写现有线程、日志和数据库；缺少新摘要字段的旧线程继续保留并通过原详情链恢复。
- 仅为仍使用历史默认图片白名单的配置增加 GIF；自定义白名单、Provider、密钥、Base URL 和持久化模型选择均保持不变。

## [0.4.1] - 2026-08-24

### Changed

- 补充 DeepSeek Harness 的实际接入说明，区分模型请求中的结构化附件与本机视觉证据分析器的内部数据传递。

### Fixed

- 修复从 Finder 启动 WorkWise 时插件市场找不到 `npm` 或 `git`，导致 npm、GitHub Skill 和 Git 目录插件均无法安装的问题。
- 修复 0.4.0 被客户端内置判定为“已撤回”、从而阻止正常应用内更新的问题；撤回版本现在只由显式运行时配置决定。

### Compatibility

- 不删除、不替换或迁移现有插件、MCP、Skill、凭据引用和用户数据；0.4.0 可直接原位升级到 0.4.1。
