# RAILWISE AI approved artwork

User-supplied optimized logo pack, received 2026-09-26. These light/dark PNGs are copied byte-for-byte from the archive; provenance.json records SHA-256 hashes.

Run `npm run generate:icons` to rebuild compatibility resource paths. Windows uses the supplied dark tiles at each ICO resolution. macOS uses the light ICNS and light/dark Dock images with standard outer padding. Runtime and startup surfaces use the matching theme tiles. Do not crop the old two-icon reference board or redraw the supplied mark.
